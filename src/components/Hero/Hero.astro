---
/**
 * * Hero with background image
 * You'll want a mobile image, and a desktop image for this.
 */

import Button from "@components/Button/Button.astro";
import heroImage from "@images/mountains.jpg";
import heroImageMobile from "@images/mountains.jpg";
import { Image } from "astro:assets";
---

<section id="hero" class="relative z-10 flex min-h-[30lvh] w-full">
	<!-- background image -->
	<div class="absolute -z-10 h-full min-h-full w-full overflow-hidden">
		<div class="absolute inset-0 bg-black/20"></div>

		<Image
			src={heroImage}
			class="hidden h-auto min-h-full w-full object-cover object-bottom sm:block"
			alt="wedding"
			aria-hidden="true"
			loading="eager"
			height={1400}
		/>
		<Image
			src={heroImageMobile}
			class="h-auto min-h-full w-full object-cover sm:hidden"
			alt="wedding"
			aria-hidden="true"
			loading="eager"
			width={1200}
		/>
	</div>

	<!-- hero items -->
	<div class="site-container relative w-full gap-10 py-44 text-center md:py-52 md:text-left">
		<!-- Hero text -->
		<div class="text-base-50 my-auto mr-auto max-w-[800px] flex-col">
			<h1
				class="font-heading-1 text-4xl font-semibold tracking-wide text-pretty uppercase drop-shadow-[0_2px_2px_rgba(0,0,0,0.4)] md:text-7xl md:leading-tight"
			>
				Dhamma Suttama
			</h1>
			<p
				class="mt-8 text-lg tracking-widest text-balance drop-shadow-[0_1px_1px_rgba(0,0,0,0.2)] md:max-w-[800px] md:text-2xl"
			>
				La vocation du Centre est d'offrir des cours de méditation Vipassana telle qu'enseignée
				par S.N. Goenka. La méditation Vipassana est une technique simple, concrète et non sectaire
				pour accéder à une réelle tranquillité d’esprit et mener une vie heureuse et paisible. Bien
				qu'issue des enseignements du Bouddha, elle peut être pratiquée par tous, sans distinction
				de race, de milieu ou de religion et elle se révélera également salutaire pour tous.
			</p>
			<!-- <div class="mt-10 flex flex-wrap justify-center gap-4 md:justify-start">
				<Button
					variant="ghost"
					arrow="right"
					href="/#contact"
					class="pl-0 text-xl text-white drop-shadow-[0_1px_1px_rgba(0,0,0,0.2)] md:text-3xl"
				>
					Get a Quote
				</Button>
			</div> -->
		</div>
	</div>
</section>
